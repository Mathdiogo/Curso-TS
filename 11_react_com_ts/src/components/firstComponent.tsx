// 3 - Criando primeiro component

import React, { ReactElement } from 'react'

type Props = {}

function firstComponent(): ReactElement {
  return (
    <div>
        Meu primeiro Componente
    </div>
  )
}

export default firstComponent